---
interface Props {
	title: string;
	tagline?: string;
}

const { tagline, title } = Astro.props;
---

<div class="hero">
	<div class="stack gap-2">
		<h1 class="title">{title}</h1>
		{tagline && <p>{tagline}</p>}
	</div>
	<slot />
</div>
